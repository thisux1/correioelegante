doctype html
html(lang="pt-BR")
  head
    meta(charset="UTF-8")
    meta(name="viewport" content="width=device-width, initial-scale=1.0")
    title= title || "Correio Elegante"
    link(rel="preconnect" href="https://fonts.googleapis.com")
    link(rel="preconnect" href="https://fonts.gstatic.com" crossorigin)
    link(href="https://fonts.googleapis.com/css2?family=Dancing+Script:wght@400..700&family=Lobster&display=swap" rel="stylesheet")
    link(rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css")
    link(rel="icon" type="image/x-icon" href="/assets/logo.png")
    script(src="https://cdn.tailwindcss.com")
    link(rel="stylesheet" href="/css/main.css")
  body(class="min-h-screen flex flex-col overflow-hidden" data-logged-in=isLoggedIn ? "true" : "false")
    include partials/header.pug
    div(class="overlay fixed inset-0 bg-black bg-opacity-50 z-10 transition-opacity duration-300") 
    div(class="dropdown-wrapper z-20") 
      div(class="dropdown-content")
        div(class="text-area")
          div(class="p-6")
            h3(class="text-2xl font-bold text-pastel-pink mb-6 text-center") Como Funciona
            div(class="space-y-6")
              div(class="flex items-start")
                span(class="text-pastel-pink font-bold text-xl mr-4") 1.
                div
                  h4(class="text-lg font-semibold mb-2") Criação da Mensagem
                  p(class="text-gray-600") Preencha o formulário com sua mensagem especial e escolha um tema que seja sua cara.
              div(class="flex items-start")
                span(class="text-pastel-pink font-bold text-xl mr-4") 2.
                div
                  h4(class="text-lg font-semibold mb-2") Personalização
                  p(class="text-gray-600") Escolha o layout da mensagem e veja uma pré-visualização antes de finalizar.
              div(class="flex items-start")
                span(class="text-pastel-pink font-bold text-xl mr-4") 3.
                div
                  h4(class="text-lg font-semibold mb-2") Pagamento
                  p(class="text-gray-600") A mensagem é enviada para processamento após a confirmação do pagamento, que é feito de forma segura via Pix, no valor de R$4.
              div(class="flex items-start")
                span(class="text-pastel-pink font-bold text-xl mr-4") 4.
                div
                  h4(class="text-lg font-semibold mb-2") Geração do QR Code
                  p(class="text-gray-600") Sua mensagem é transformada em uma URL única e convertida em um QR Code que será impresso e entregue a você para presentear seu amado(a).
              div(class="flex items-start")
                span(class="text-pastel-pink font-bold text-xl mr-4") 5.
                div
                  h4(class="text-lg font-semibold mb-2") Visualização Final
                  p(class="text-gray-600") O destinatário precisará apenas escanear o QR Code para visualizar o seu presente digital e se encantar com a criativa surpresa.
        div(class="footer-section")
          div(class="scroll-indicator")
            i(class="fa fa-arrow-down")
          div(class="text-center mt-4")
            button(class="close-dropdown learn-more") Fechar
    block content
    block scripts
    script(src="/js/header.js")